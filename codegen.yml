schema:
  - ${GRAPHQL_API_ENDPOINT}
documents:
  - 'src/queries/*.{ts,gql}'
generates:
  src/generated/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-react-apollo'
    config:
      scalars:
        ISBN: string
        URL: string
